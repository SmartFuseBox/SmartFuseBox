<?xml version="1.0" encoding="utf-8" ?>
<views:BasePowerControlHubContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:PowerControlHubApp.Views"
             xmlns:vm="clr-namespace:PowerControlHubApp.ViewModels"
             xmlns:localization="clr-namespace:PowerControlHubApp.Resources.Localization"
             x:Class="PowerControlHubApp.Views.LocationSettingsPage"
             Title="{x:Static localization:AppResources.LocationSettingsTitle}"
             BackgroundColor="{DynamicResource AppPageBg}">

    <Grid RowDefinitions="Auto,Auto,*,Auto" BackgroundColor="{DynamicResource AppPageBg}">

        <views:TopBarView Grid.Row="0" Title="{x:Static localization:AppResources.LocationSettingsTitle}" ShowBackButton="True" />

        <views:NotConnectedBanner Grid.Row="1" />

        <ScrollView Grid.Row="2" IsVisible="{Binding IsConnected}">
            <VerticalStackLayout Padding="20" Spacing="12">

                <!-- Location Name -->
                <Border BackgroundColor="{DynamicResource AppHelpCardBg}"
                        Stroke="{DynamicResource AppHelpCardStroke}"
                        StrokeShape="RoundRectangle 8" Padding="14">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="{x:Static localization:AppResources.NameLabel}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontAttributes="Bold"
                               FontSize="13" />

                        <Entry Text="{Binding LocationName}"
                               Placeholder="{x:Static localization:AppResources.LocationNamePlaceholder}"
                               TextColor="{DynamicResource AppEntryText}"
                               PlaceholderColor="{DynamicResource AppPlaceholderText}"
                               BackgroundColor="{DynamicResource AppEntryBg}"
                               ClearButtonVisibility="WhileEditing" />
                    </VerticalStackLayout>
                </Border>

                <!-- Location type -->
                <Border BackgroundColor="{DynamicResource AppHelpCardBg}"
                        Stroke="{DynamicResource AppHelpCardStroke}"
                        StrokeShape="RoundRectangle 8" Padding="14">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="{x:Static localization:AppResources.LocationType}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontAttributes="Bold"
                               FontSize="13" />

                        <Picker ItemsSource="{Binding LocationTypes}"
                                ItemDisplayBinding="{Binding Description}"
                                SelectedIndex="{Binding SelectedIndex}"
                                Title="{x:Static localization:AppResources.SelectLocationType}"
                                Unfocused="OnPickerUnfocused" />
                    </VerticalStackLayout>
                </Border>

                <!-- Boat-specific fields (MMSI / CallSign / HomePort) -->
                <Border BackgroundColor="{DynamicResource AppHelpCardBg}"
                        Stroke="{DynamicResource AppHelpCardStroke}"
                        StrokeShape="RoundRectangle 8" Padding="14"
                        Margin="0,8,0,0"
                        IsVisible="{Binding IsBoat}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="{x:Static localization:AppResources.BoatDetails}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontSize="13" />
                        <Label Text="{x:Static localization:AppResources.MmsiLabel}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontSize="11" />
                        <Entry Text="{Binding Mmsi}"
                               Placeholder="{x:Static localization:AppResources.MmsiLabel}"
                               TextColor="{DynamicResource AppEntryText}"
                               PlaceholderColor="{DynamicResource AppPlaceholderText}"
                               BackgroundColor="{DynamicResource AppEntryBg}"
                               ClearButtonVisibility="WhileEditing"
                               Keyboard="Numeric"
                               MaxLength="9" />

                        <Label Text="{x:Static localization:AppResources.CallSignLabel}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontSize="11" />
                        <Entry Text="{Binding CallSign}"
                               Placeholder="{x:Static localization:AppResources.CallSignLabel}"
                               TextColor="{DynamicResource AppEntryText}"
                               PlaceholderColor="{DynamicResource AppPlaceholderText}"
                               BackgroundColor="{DynamicResource AppEntryBg}"
                               ClearButtonVisibility="WhileEditing"
                               MaxLength="9" />

                        <Label Text="{x:Static localization:AppResources.HomePortLabel}"
                               TextColor="{DynamicResource AppLabelMuted}"
                               FontSize="11" />
                        <Entry Text="{Binding HomePort}"
                               Placeholder="{x:Static localization:AppResources.HomePortLabel}"
                               TextColor="{DynamicResource AppEntryText}"
                               PlaceholderColor="{DynamicResource AppPlaceholderText}"
                               BackgroundColor="{DynamicResource AppEntryBg}"
                               ClearButtonVisibility="WhileEditing" />
                    </VerticalStackLayout>
                </Border>

                <Button Text="{x:Static localization:AppResources.Save}"
                        Command="{Binding SaveCommand}"
                        BackgroundColor="{DynamicResource AppAccent}"
                        TextColor="White"
                        CornerRadius="8"
                        Padding="10,8" />

                <Label Text="{Binding StatusMessage}"
                       TextColor="{DynamicResource AppAccent}"
                       FontSize="13"
                       HorizontalTextAlignment="Center"
                       IsVisible="{Binding HasStatus}" />
            </VerticalStackLayout>
        </ScrollView>

        <views:StatusBarView Grid.Row="3" />
    </Grid>

</views:BasePowerControlHubContentPage>
